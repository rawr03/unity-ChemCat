using System.Collections;
using System.Collections.Generic;
using Unity.VisualScripting;
using UnityEditor.SceneManagement;
using UnityEngine;
using UnityEngine.UI;

public class DB_draft : MonoBehaviour
{
    /*
public enum Expression
{
    smile,
    openmouthsmile,
    closedsmile,
    angry,
    sad,
    scared,
    smart,
    cat,
    meh
}
            // Image.gameObject.GetComponent<Image>().sprite = sprite;
*/    // public Character character;
      //public Expression[] expresssions;

    /*
     * public Expression Expression
    {
        set
        {
            if (value == Expression.smile)
            {
                sprite = character.smile;
            }
            else if (value == Expression.openmouthsmile)
            {
                sprite = character.openmouthsmile;
            }
            else if (value == Expression.angry)
            {
                sprite = character.angry;
            }
            else if (value == Expression.sad)
            {
                sprite = character.sad;
            }
            else if (value == Expression.scared)
            {
                sprite = character.scared;
            }
            else if (value == Expression.smart)
            {
                sprite = character.smart;
            }
            else if (value == Expression.cat)
            {
                sprite = character.cat;
            }
            else if (value == Expression.closedsmile)
            {
                sprite = character.closedsmile;
            }
            else
            {
                sprite = character.meh;
            }
            //character.sprite = sprite;

        }
    }
     * 
     * 
    /*
    public GameObject image;
    public Sprite sprite;

    //image.gameObject.GetComponent<Image>().sprite = expression;

    /*
    public void ReecordinArray(string problem, Sprite sprite)
    {
        // display sprite according to text
    }
    

    public void DisplaySprite(Sprite expression)
    {
        image.gameObject.GetComponent<Image>().sprite = expression;
    }

        public GameObject Image;
    public Character character;
    public Expression expression;
    private Sprite sprite;
    */

}