using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Confirm : MonoBehaviour
{
    // Start is called before the first frame update

    /*
    public GameObject confirm, input1, input2, input3, input4;

    public int RT_coef1;
    public int RT_coef2; 
    public int PD_coef1; 
    public int PD_coef2;
    */
    // Update is called once per frame
    void Update()
    {
       /*
        if (input1.text = RT_coef1 && input2 = RT_coef2)
        {
            
        }
       */
    }
       
}
